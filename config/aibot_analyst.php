<?php

/*
 * TPIX TRADE — ให้ AI เข้าร่วมตัดสินใจเข้าไม้/ออกไม้ เป็นรอบ.
 *
 * ต่างจาก `aibot_advisor.php` ที่เป็น "คำแนะนำให้ผู้ใช้อ่าน" — ตัวนี้คือมุมมองที่
 * **บอทเอาไปใช้จริง** จึงต้องมีขอบเขตที่รัดกุมกว่ามาก
 *
 * ═══ ขอบเขตอำนาจของ AI (สำคัญที่สุดในไฟล์นี้) ═══
 *
 * AI **ทำได้**:
 *   - ห้ามเข้าไม้ใหม่ในเหรียญที่มันมองว่าแย่           (veto)
 *   - ลด/เพิ่มขนาดไม้ ภายในเพดานที่ตั้งไว้ด้านล่าง      (bias)
 *   - ขอให้ปิดไม้ที่ถืออยู่                              (exit)
 *   - ลดเกณฑ์ความมั่นใจของกลยุทธ์ เมื่อมันมั่นใจมาก      (assist)
 *   - จัดอันดับว่าเหรียญไหนน่าเทรดที่สุด                 (เลือกเหรียญ)
 *
 * AI **ทำไม่ได้**:
 *   - สั่งซื้อเองทั้งที่กฎของกลยุทธ์ยังไม่ให้สัญญาณ
 *
 * เหตุผลของเส้นแบ่งนี้: คำตอบของ LLM ไม่คงที่ ถามคำถามเดิมสองครั้งได้คนละคำตอบ
 * ถ้าให้มันเป็นต้นทางของคำสั่งซื้อโดยตรง จะไม่มีทางย้อนตรวจได้ว่าทำไมถึงเสียเงิน
 * และทดสอบย้อนหลังก็ไม่ได้ ให้มัน "กรองและถ่วงน้ำหนัก" สัญญาณจากกฎแทน —
 * ได้ประโยชน์จากการอ่านข่าวและภาพรวม โดยยังเหลือร่องรอยให้ตรวจสอบครบ
 *
 * Developed by Xman Studio.
 */

return [
    /* สวิตช์ใหญ่ — ปิดแล้วบอทกลับไปใช้กฎล้วนทันที ไม่มีอะไรพัง */
    'enabled' => env('AIBOT_ANALYST_ENABLED', false),

    /*
    |--------------------------------------------------------------------------
    | โหมดเงา — AI คิดและถูกบันทึก แต่ห้ามแตะการเทรด
    |--------------------------------------------------------------------------
    | **ปิดไว้โดยปริยาย** — เจ้าของยืนยัน 28 ส.ค. 2026 ว่าให้ปิดเฉพาะรอบ 15 นาที
    | ส่วน AI ให้ทำงานจริงกับรอบใหญ่ ไม่ใช่แค่ดูเฉยๆ
    |
    | เก็บสวิตช์นี้ไว้เพราะมีประโยชน์ในสองกรณี:
    |   - อยากลองเปลี่ยน prompt หรือโมเดล โดยไม่ให้กระทบเงินระหว่างทดลอง
    |   - รายงานบอกว่ายังไม่ชนะต้นทุน แต่ยังไม่อยากถอดออกทั้งระบบ
    |
    | เปิดแล้ว: รอบวิเคราะห์ยังเดินและบันทึกครบเหมือนเดิม (MarketAnalyst ไม่ดูค่านี้
    | โดยตั้งใจ) แต่ AiViewGate คืนค่าว่างเสมอ → กฎล้วนเป็นคนตัดสินทั้งหมด
    |
    | ประเมินผลได้ทุกเมื่อด้วย `php artisan aibot:analyst-report --days=2`
    | ซึ่งให้คะแนนจาก "คำตัดสินของ AI เทียบราคาจริง" ไม่ได้ดูกำไรพอร์ต จึงใช้ได้
    | ทั้งตอนเปิดและตอนปิดโหมดเงา
    */
    'shadow_mode' => env('AIBOT_ANALYST_SHADOW', false),

    /* ผู้ให้บริการ — ใช้คีย์ชุดเดียวกับที่ปรึกษา (AdvisorSettings) ไม่ต้องกรอกซ้ำ */
    'provider' => env('AIBOT_ANALYST_PROVIDER', 'openai'),

    /*
    |--------------------------------------------------------------------------
    | โมเดล
    |--------------------------------------------------------------------------
    | ⚠️ บทเรียนจริงจากโปรเจกต์นี้ (21 ส.ค. 2026): ค่า "ชื่อโมเดล" เน่าได้เอง
    |    โดยไม่มีใครแตะ — Groq ถอด Llama ออกหมด งานเจนคอนเทนต์ล้มวันละ 2 รอบ
    |    เงียบๆ ตั้งแต่ 18 ส.ค. กว่าจะรู้ก็ต้องไปไล่อ่าน log
    |
    |    ตัวรันจึงตรวจชื่อโมเดลกับ /models ก่อนใช้ และล้มแบบดังถ้าไม่มี
    |    (ดู MarketAnalyst::assertModelAvailable)
    */
    'model' => env('AIBOT_ANALYST_MODEL', 'gpt-4o-mini'),

    'endpoint' => env('AIBOT_ANALYST_ENDPOINT', 'https://api.openai.com/v1/chat/completions'),
    'models_endpoint' => env('AIBOT_ANALYST_MODELS_ENDPOINT', 'https://api.openai.com/v1/models'),
    'timeout' => 60,

    /*
    |--------------------------------------------------------------------------
    | สองจังหวะตามแพลน (เจ้าของกำหนด 28 ส.ค. 2026)
    |--------------------------------------------------------------------------
    | strategic = รอบใหญ่ ทุกแพลนได้ · มองภาพกว้าง จัดอันดับเหรียญ
    | tactical  = รอบสั้น เฉพาะแพลนสูง · ปรับท่าทีตามข่าวที่เพิ่งเข้า
    |
    | `ttl_minutes` ต้องยาวกว่าคาบเล็กน้อย เผื่อรอบถัดไปยิงช้าหรือ API ล่มชั่วคราว
    | แต่ห้ามยาวเกินไป — มุมมองที่หมดอายุแล้วดีกว่ามุมมองเก่าที่ยังถูกใช้อยู่
    */
    'scopes' => [
        'strategic' => [
            'enabled' => env('AIBOT_ANALYST_STRATEGIC', true),
            'every_minutes' => 240,
            'ttl_minutes' => 300,
            'min_tier' => 'free',
            'max_coins' => 70,
            'max_headlines' => 30,
        ],

        /*
         * รอบสั้น — **ยั้งไว้ก่อนตามคำสั่งเจ้าของ 28 ส.ค. 2026**
         *
         * เหตุผล: 96 ครั้ง/วัน คิดเป็นราว 8 ใน 9 ล้านโทเคนต่อเดือนของทั้งระบบ
         * และคีย์มาจากพูลที่บิลรวมกับงานอื่น — จ่ายค่านี้ก่อนจะรู้ว่า AI ฉลาดพอ
         * หรือเปล่ายังไม่คุ้ม เก็บผลรอบใหญ่ให้พอตัดสินก่อน แล้วค่อยเปิด
         *
         * เปิดเมื่อไหร่ก็แค่ AIBOT_ANALYST_TACTICAL=true — โค้ดพร้อมอยู่แล้ว
         */
        'tactical' => [
            'enabled' => env('AIBOT_ANALYST_TACTICAL', false),
            'every_minutes' => 15,
            'ttl_minutes' => 25,
            'min_tier' => 'pro',
            'max_coins' => 25,
            'max_headlines' => 15,
        ],
    ],

    /*
    |--------------------------------------------------------------------------
    | เพดานอำนาจ — AI ขยับได้แค่ไหน
    |--------------------------------------------------------------------------
    | ตัวเลขพวกนี้คือสิ่งที่กันไม่ให้คำตอบเพี้ยนครั้งเดียวล้างพอร์ต
    |
    | size_multiplier: AI ขอเพิ่มขนาดไม้ได้ไม่เกิน 1.3 เท่า และลดได้ถึง 0
    |   ไม่สมมาตรโดยตั้งใจ — ให้มัน "เบรก" ได้แรงกว่า "เหยียบ" เพราะความเสียหาย
    |   จากการเข้าไม้ผิดจังหวะกู้คืนยากกว่าการพลาดโอกาส
    |
    | confidence_relief: เมื่อ AI มั่นใจขาขึ้นมาก จะลดเกณฑ์ความมั่นใจของกลยุทธ์
    |   ได้ไม่เกินกี่จุด (จาก 100) — 8 จุดพอให้เกิดไม้ที่กฎเกือบผ่านอยู่แล้ว
    |   แต่ไม่พอให้สัญญาณอ่อนๆ ทะลุผ่าน
    */
    'limits' => [
        'size_multiplier_max' => 1.3,
        'size_multiplier_min' => 0.0,
        'confidence_relief_max' => 8.0,

        /*
         * ต้องมั่นใจเกินเท่านี้ AI ถึงมีสิทธิ์ออกเสียง
         *
         * ต่ำกว่านี้ = ถือว่ามันก็ไม่รู้ ปล่อยให้กฎทำงานไปตามปกติ ดีกว่าเอา
         * ความเห็นที่ตัวมันเองไม่มั่นใจมาถ่วงการตัดสินใจเรื่องเงิน
         */
        'min_confidence' => 0.55,

        /*
         * AI สั่งเทออกได้ต่อเมื่อมั่นใจถึงระดับนี้
         *
         * สูงกว่า min_confidence มากเพราะการขายทิ้งมีต้นทุนจริง 0.36% ต่อรอบ
         * ถ้าให้สั่งขายง่ายๆ ค่าธรรมเนียมจะกินพอร์ตมากกว่าที่มันช่วยหนีทัน
         * (เหตุผลเดียวกับเกณฑ์เทออกของด่านข่าว — ดู config/aibot_risk.php)
         */
        'exit_confidence' => 0.75,

        /*
         * เกณฑ์เมื่อมี "ตาราง calibration" (อัตราทายถูกจริงจากประวัติ) — ใช้แทนตัวเลขข้างบน
         *
         * ออดิท 2 ก.ย. 2026: ความมั่นใจที่ AI รายงานเองกลับหัวกับผลจริง (≥0.8 ถูก 35%)
         * ตัวเลขที่ควรคุมเงินคือ "ที่ผ่านมามันถูกกี่ครั้ง" ไม่ใช่ "มันบอกว่ามั่นใจแค่ไหน"
         *   calibrated_exit_min   : ประวัติ exit ในช่วงความมั่นใจนี้ต้องถูก ≥ เท่านี้ ถึงสั่งปิดไม้ได้
         *   calibrated_relief_min : ประวัติ buy ต้องถูก ≥ เท่านี้ ถึงจะผ่อนเกณฑ์ให้กลยุทธ์
         * ช่องที่ยังมีตัวอย่างไม่พอ → ถอยไปใช้ min_confidence / exit_confidence ตามเดิม
         */
        'calibrated_exit_min' => 0.55,
        'calibrated_relief_min' => 0.5,
        // ประวัติ avoid ต้องถูก ≥ เท่านี้ ถึงห้ามเข้าไม้ได้ (ออดิท R3: avoid ถูกแค่ 21% ที่ 24 ชม.)
        'calibrated_avoid_min' => 0.5,
    ],

    /*
    |--------------------------------------------------------------------------
    | อำนาจของ AI ต้องมาจากฝีมือที่วัดได้ (ดู AnalystCalibration::skill)
    |--------------------------------------------------------------------------
    | Brier ของ p_up_24h ≥ max_brier บนตัวอย่าง ≥ min_samples = AI ทายไม่ดีกว่าโยนเหรียญ
    | → ดูได้อย่างเดียว: ห้ามแตะขนาดไม้ ห้ามห้ามเข้าไม้ ห้ามสั่งปิด ห้ามย้ายเหรียญ
    | รอบวิเคราะห์ยังเดินและถูกให้คะแนนต่อ — ฝีมือดีขึ้นเมื่อไหร่ได้อำนาจคืนเองในรอบ
    | calibrate ถัดไป
    |
    | ออดิท R3 (2 → 23 ก.ย. 2026): Brier 0.292 จาก 260 คำตัดสิน — ตัวที่ต้องกันคือแบบนี้
    | 0.25 = คำตอบ "0.5 ทุกครั้ง" · 60 ตัวอย่าง ≈ 5 วันของรอบ strategic
    */
    'authority' => [
        'enabled' => (bool) env('AIBOT_ANALYST_EARN_AUTHORITY', true),
        'min_samples' => 60,
        'max_brier' => 0.25,
        // ถูกลดสิทธิ์แล้วต้องได้ Brier < max_brier − regain_margin ถึงได้อำนาจคืน (กันสลับทุกวัน)
        'regain_margin' => 0.01,
    ],

    /*
    |--------------------------------------------------------------------------
    | เครื่องมือของ AI — สิ่งที่มันได้เห็นต่อรอบ (ดู MarketContext / CoinSnapshot)
    |--------------------------------------------------------------------------
    | ออดิท 2 ก.ย. 2026: AI ได้แค่ราคาปัจจุบัน + %24 ชม. + จำนวนข่าว แล้วต้องทาย
    | 4-24 ชม. ข้างหน้า → buy 21 ครั้งราคาไปต่อ −44 bps ตัวเลขข้างล่างคุมว่าจะ
    | เติมภาพราคา (แท่ง 4 ชม. 200 แท่ง · 1 คำขอ/เหรียญ) ให้กี่เหรียญ และให้มัน
    | เห็นผลของคำตัดสินตัวเองย้อนหลังกี่วัน
    */
    'snapshots' => [
        'max_coins' => 40,        // เหรียญที่ได้ภาพราคาเต็ม (เรียงตามความสำคัญก่อนตัด)
    ],
    'track_record' => [
        'days' => 3,              // ย้อนดูคำตัดสินตัวเองกี่วัน
        'recent_calls' => 6,      // แสดงคำตัดสินล่าสุดกี่รายการแบบละเอียด
    ],

    /*
    |--------------------------------------------------------------------------
    | calibration — สร้างจากประวัติทุกวันด้วย aibot:calibrate (ดู AnalystCalibration)
    |--------------------------------------------------------------------------
    */
    'calibration' => [
        'days' => 14,
        /*
         * 24 ชม. ไม่ใช่ 4 — (1) prompt ขอ p_up_24h ความน่าจะเป็นต้องวัดกับผลของคำถามเดียวกัน
         * (2) ไม้ของบอทถือกันราว 1–4 วัน (R3: 6–220 ชม.) ท่าทีที่ถูกแค่ 4 ชม. แล้วกลับข้าง
         *     ไม่ได้ช่วยไม้ที่ถูกห้าม/ผ่อนเกณฑ์ตามมัน — R3: buy ขึ้น +45 bps ที่ 4 ชม.
         *     แต่มัธยฐาน −83 bps ที่ 24 ชม.
         */
        'horizon_hours' => 24,
        'min_samples' => 15,      // ต่อช่อง (ท่าที × ช่วงความมั่นใจ) — น้อยกว่านี้คือความบังเอิญ
        'ttl_hours' => 36,        // ยาวกว่ารอบสร้าง (24 ชม.) เผื่อ cron พลาดหนึ่งวัน
        // สำเนาบนดิสก์ — cache ถูกล้างทุก deploy · ว่าง = ไม่เก็บ (ชุดเทสต์ตั้งเป็นว่าง)
        'store' => env('AIBOT_CALIBRATION_STORE', storage_path('app/aibot/calibration.json')),
    ],

    /*
    |--------------------------------------------------------------------------
    | เลือกเหรียญอัตโนมัติ
    |--------------------------------------------------------------------------
    | บอทที่ตั้ง pair = 'AUTO' จะเทรดคู่ที่ AI จัดอันดับไว้สูงสุดในรอบนั้น
    |
    | `min_hold_minutes` กันการสลับเหรียญถี่เกินไป — เปลี่ยนเหรียญทุกรอบ 15 นาที
    | แปลว่าจ่ายค่าเข้าออก 0.36% ทุกครั้งที่อันดับสลับ ซึ่งกินกำไรหมดก่อนที่
    | การเลือกเหรียญจะได้พิสูจน์ตัวเอง
    */
    'auto_pair' => [
        'enabled' => true,
        'placeholder' => 'AUTO',
        'quote' => 'USDT',
        'min_hold_minutes' => 240,
        'shortlist_size' => 5,
    ],

    /* โควตากันค่า API บานปลาย — นับรวมทุก scope */
    'max_calls_per_day' => env('AIBOT_ANALYST_MAX_CALLS', 200),

    /*
    |--------------------------------------------------------------------------
    | เก็บมุมมองย้อนหลังกี่วัน
    |--------------------------------------------------------------------------
    | แต่ละแถวพก `prompt` กับ `raw_response` เป็น longtext (~8KB/ใบ) ไม่มีตัวลบ
    | แล้วตารางจะโตไม่มีที่สิ้นสุด — รอบใหญ่ 6 ใบ/วันยังไม่เท่าไร แต่วันที่เปิด
    | รอบสั้นด้วยจะเป็น 102 ใบ/วัน ≈ 800KB/วัน ซึ่งสะสมแล้วเห็นผลใน DB จริง
    |
    | 30 วันเลือกให้ **ยาวกว่าหน้าต่างประเมินที่ยาวที่สุดที่จะใช้จริงมากๆ**
    | (เจ้าของขอเก็บ 4 วัน) — ตัดสั้นกว่าหน้าต่างเมื่อไหร่ รายงานจะคิดจากข้อมูล
    | ไม่ครบโดยไม่มีอะไรบอก ซึ่งแย่กว่าปล่อยให้ตารางโตอีก
    */
    'retention_days' => env('AIBOT_ANALYST_RETENTION_DAYS', 30),
];
