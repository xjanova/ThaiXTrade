<?php

return [

    /*
    |--------------------------------------------------------------------------
    | ฐานแผนที่ (หน้ารายงานตำแหน่งมาสเตอร์โหนด /validators)
    |--------------------------------------------------------------------------
    |
    | แผนที่วาดสองชั้น:
    |   ชั้นล่าง  รูปร่างประเทศจากไฟล์ในโปรเจกต์ (Natural Earth) — ไม่มีวันหมดอายุ
    |   ชั้นบน   ไทล์ภาพจากผู้ให้บริการ — ได้หน้าตาละเอียดและซูมได้ลึก
    |
    | ถ้าไทล์โหลดไม่ได้ (คีย์หมดอายุ โควตาเต็ม เน็ตล่ม) หน้าเว็บจะถอดชั้นบนออกเอง
    | แล้วเหลือแผนที่ประเทศ + หมุดโหนด ไม่กลายเป็นจอเปล่า
    |
    | ⚠️ repo นี้เป็นสาธารณะ — ห้ามใส่คีย์ลงไฟล์นี้ ให้ใส่ใน .env เท่านั้น
    |
    | ปลายเดือน ส.ค. 2026 CARTO เปลี่ยนมาบังคับใช้ API key: ไทล์ที่เรียกโดยไม่มีคีย์
    | ยังตอบ 200 แต่เป็นภาพหม่น ๆ ที่มีลายน้ำ "API KEY REQUIRED" พาดทั้งแผนที่
    | (ไม่ได้พังเป็น error จึงไม่มีอะไรแจ้งเตือน เห็นได้จากหน้าเว็บอย่างเดียว)
    |
    | ขอคีย์ฟรีที่ https://carto.com/basemaps/apikey (ฟรี 5 ล้านไทล์/เดือน)
    | แล้วต่อท้าย URL ด้วย ?key=<คีย์>
    | แก้ .env บน prod แล้วต้อง `php artisan config:cache` เสมอ
    |
    */

    'tile_url' => env('MAP_TILE_URL', ''),

    'tile_subdomains' => env('MAP_TILE_SUBDOMAINS', 'abcd'),

    'tile_max_zoom' => (int) env('MAP_TILE_MAX_ZOOM', 15),

    'tile_attribution' => env(
        'MAP_TILE_ATTRIBUTION',
        '&copy; <a href="https://carto.com" target="_blank" rel="noopener">CARTO</a> &copy; <a href="https://osm.org" target="_blank" rel="noopener">OSM</a>'
    ),

];
