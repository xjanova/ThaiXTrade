<?php

/*
 * TPIX TRADE — ค่าตั้งต้นของระบบยืนยันตัวตน.
 *
 * ไฟล์นี้เก็บ "โครง" ที่โค้ดต้องรู้ตั้งแต่ตอนบูต — รายชื่อด่าน ชนิดไฟล์ที่รับ ขนาดสูงสุด
 * ส่วน "สวิตช์" ว่าด่านไหนเปิดอยู่ อ่านจาก site_settings เพราะเจ้าของต้องกดเปลี่ยนเองได้
 * โดยไม่ต้องแก้โค้ดหรือ deploy ใหม่ (ดู KycGate)
 *
 * Developed by Xman Studio.
 */

return [
    /*
    |--------------------------------------------------------------------------
    | ด่านที่เปิด/ปิดได้
    |--------------------------------------------------------------------------
    |
    | เจ้าของสั่งว่า "มีทุกส่วน เปิดปิดได้ที่สำคัญๆ" — ด่านจึงมีครบทุกฟีเจอร์
    | แต่ค่าตั้งต้นปิดหมด ยกเว้นสองอันที่สั่งมาตรงๆ คือเช่าบอท AI กับสร้างเหรียญ
    |
    | ⚠️ ค่า `default` ในนี้ใช้เฉพาะตอนที่ยังไม่เคยตั้งใน site_settings
    |    ตั้งแล้วค่าในฐานข้อมูลชนะเสมอ ไม่งั้น deploy ทับค่าที่เจ้าของกดไว้
    |
    | `level` = ระดับที่ต้องผ่านถึงจะใช้ฟีเจอร์นี้ได้
    |    basic    = บัตรประชาชน + เซลฟี่คู่บัตร
    |    enhanced = เพิ่มหลังบัตร + หลักฐานที่อยู่
    |
    */

    'features' => [
        'ai_bot' => [
            'label_th' => 'เช่าบอทเทรด AI',
            'label_en' => 'AI trading bot',
            'desc_th' => 'ให้บอทส่งสัญญาณเทรดแทน',
            'default' => true,
            'level' => 'basic',
        ],
        'token_factory' => [
            'label_th' => 'สร้างเหรียญ',
            'label_en' => 'Token factory',
            'desc_th' => 'ออกเหรียญใหม่บนเชน',
            'default' => true,
            'level' => 'basic',
        ],
        'token_sale' => [
            'label_th' => 'ซื้อเหรียญในรอบขาย',
            'label_en' => 'Token sale',
            'desc_th' => 'ซื้อ TPIX จากรอบขายของโครงการ',
            'default' => false,
            'level' => 'basic',
        ],
        'trading' => [
            'label_th' => 'เทรด / สวอป',
            'label_en' => 'Trading & swap',
            'desc_th' => 'วางไม้ซื้อขายและแลกเหรียญ',
            'default' => false,
            'level' => 'basic',
        ],
        'bridge' => [
            'label_th' => 'สะพานข้ามเชน',
            'label_en' => 'Bridge',
            'desc_th' => 'ย้ายเหรียญข้ามเครือข่าย',
            'default' => false,
            'level' => 'basic',
        ],
        'masternode' => [
            'label_th' => 'มาสเตอร์โหนด / สเตค',
            'label_en' => 'Masternode & staking',
            'desc_th' => 'เปิดโหนดและวางค้ำประกัน',
            'default' => false,
            'level' => 'basic',
        ],
    ],

    /*
    |--------------------------------------------------------------------------
    | ข้อความความยินยอม
    |--------------------------------------------------------------------------
    |
    | เลขเวอร์ชันถูกบันทึกลงใบคำขอทุกใบ เพื่อพิสูจน์ทีหลังได้ว่าตอนกดยินยอม
    | เขาเห็นข้อความชุดไหน — แก้ข้อความแล้วต้องขยับเลขนี้ด้วยเสมอ
    |
    | ⚠️ แก้ข้อความโดยไม่ขยับเวอร์ชัน = อ้างในภายหลังไม่ได้ว่าเขายินยอมอะไร
    |
    */

    'consent_version' => '1.0',

    /*
    |--------------------------------------------------------------------------
    | ระยะเก็บข้อมูล (วัน)
    |--------------------------------------------------------------------------
    |
    | PDPA ให้เก็บได้เท่าที่จำเป็นตามวัตถุประสงค์ ไม่ใช่ตลอดกาล
    | ครบกำหนดแล้ว kyc:purge ลบไฟล์ทิ้งและล้างฟิลด์ที่ระบุตัวบุคคล
    | เหลือไว้แค่โครงใบว่าเคยตรวจเมื่อไหร่ ใครตรวจ ผลเป็นอย่างไร
    |
    | 1825 วัน = 5 ปี ตามอายุความที่หน่วยงานกำกับด้านการเงินมักกำหนด
    | เจ้าของปรับได้ที่หลังบ้านถ้าที่ปรึกษากฎหมายบอกตัวเลขอื่น
    |
    */

    'retention_days' => 1825,

    /*
    |--------------------------------------------------------------------------
    | ไฟล์ที่รับ
    |--------------------------------------------------------------------------
    |
    | ⚠️ ตรวจ mime จากเนื้อไฟล์จริง ไม่ใช่จากนามสกุลหรือ Content-Type ที่ client ส่งมา
    |    ทั้งสองอย่างนั้นผู้ส่งแก้ได้ตามใจ — ดู KycService::storeDocument()
    |
    | ไม่รับ PDF และไม่รับ SVG โดยตั้งใจ:
    |    - SVG คือ XML ที่ฝัง <script> ได้ เปิดดูในเบราว์เซอร์แอดมิน = XSS
    |    - PDF มี JS ในตัวเองและ parser มีประวัติช่องโหว่ยาว
    |    เราต้องการแค่ "รูปถ่ายบัตร" ซึ่ง jpg/png/webp พอแล้ว
    |
    */

    'uploads' => [
        'max_size_kb' => 8192,
        'mimes' => ['image/jpeg', 'image/png', 'image/webp'],
        'extensions' => ['jpg', 'jpeg', 'png', 'webp'],
        'disk' => 'kyc',
    ],

    /*
    |--------------------------------------------------------------------------
    | ข้อจำกัดการยื่น
    |--------------------------------------------------------------------------
    |
    | กันคนยื่นรัวๆ จนคิวตรวจล้น และกันคนเดียวเปิดหลายบัญชีด้วยบัตรใบเดียว
    |
    */

    'limits' => [
        // ยื่นใหม่ได้กี่ครั้งใน 24 ชม. (นับเฉพาะที่ถูกปฏิเสธไปแล้ว)
        'submissions_per_day' => 3,
        // บัตรใบเดียวผูกได้กี่บัญชี — เกินแล้วขึ้นธงให้ทีมงานดู ไม่บล็อกอัตโนมัติ
        'accounts_per_id' => 1,
    ],
];
