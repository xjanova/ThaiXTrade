# TPIX TRADE — Build secrets template
# Copy to .env.local (which is gitignored) and fill in your own values

# Reown AppKit / WalletConnect v2 project ID — required for external wallets
# Sign up free at https://cloud.reown.com
REOWN_PROJECT_ID=
